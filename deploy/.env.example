# 复制为服务器 /opt/hiking-manage-back/.env，并填写实际值。
# JWT_SECRET 使用至少 32 字节的随机值，JWT_SECRET 和 AUTH_DEFAULT_PASSWORD 均不能为空。
JWT_SECRET=
AUTH_DEFAULT_PASSWORD=
# JWT 有效期，单位为分钟，不能为空。
JWT_EXPIRE_MINUTES=120
# 使用容器内路径；其它容器需挂载同一个 hiking-data 数据卷以共享数据库目录。
SPRING_DATASOURCE_URL=jdbc:sqlite:/app/data/chatbi.db
